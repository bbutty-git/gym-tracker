{
  "name": "gym-tracker-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Remote MCP server that lets a Claude chat read and write the Gym Tracker workout log.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "compile": "tsc --outDir dist --noEmit false",
    "typecheck": "tsc --noEmit",
    "test": "npm run compile && node test/e2e.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@supabase/supabase-js": "^2.58.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
